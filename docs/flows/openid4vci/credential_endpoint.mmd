%%{init: {"htmlLabels": false, "flowchart": {"htmlLabels": false}}}%%
flowchart TD
    start[Request /credential] --> method{POST request?}
    method -- no --> not_found["`**HTTP 404** not found`"]
    method -- yes --> host{Host present and valid?}
    host -- no --> request_error["`**HTTP 400** invalid_request`"]
    host -- yes --> bearer{Bearer access token valid?}
    bearer -- no --> bearer_error["`**HTTP 401** invalid_token`"]
    bearer -- yes --> media{Content-Type is application/json?}
    media -- no --> credential_error["`**HTTP 400** invalid_credential_request`"]
    media -- yes --> configuration{Configuration present and known?}
    configuration -- no --> credential_error
    configuration -- yes --> authorized{Token authorizes configuration?}
    authorized -- no --> bearer_error
    authorized -- yes --> generate{Ed25519 JWT VC generated?}
    generate -- no --> generation_error["`**HTTP 400** invalid_token_response`"]
    generate -- yes --> success["`**HTTP 200** credentials array`"]
