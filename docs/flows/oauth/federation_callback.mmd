%%{init: {"htmlLabels": false, "flowchart": {"htmlLabels": false}}}%%
flowchart TD
    start[Request /federation_callback] --> method{HTTP method GET?}
    method -- no --> not_found["`**HTTP 404** not found`"]
    method -- yes --> state{Encrypted state validates client and redirect URI?}
    state -- no --> invalid_state["`**HTTP 400** local invalid_request: invalid or expired state`"]
    state -- yes --> response{Authorization response?}
    response -- code and error --> redirect_error["`**HTTP 302** client redirect with error, error_description, and state`"]
    response -- empty code or error --> redirect_error
    response -- code --> token{Token request and response valid?}
    token -- no --> redirect_error
    token -- yes --> identity{Identity endpoint responses and configured claims valid?}
    identity -- no --> redirect_error
    identity -- yes --> authorize{Continued authorize response generated?}
    authorize -- no --> redirect_error
    authorize -- yes --> accepted["`**HTTP 302** downstream authorize response`"]
    response -- error --> redirect_error
    response -- missing --> redirect_error
